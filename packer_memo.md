packer
====


## install

```bash
$ sudo pacman -S grep sed bash curl jshon expac

$ git clone https://aur.archlinux.org/packages/packer packer
$ cd packer
$ makepkg
$ sudo pacman -U packer-*-any.pkg.tar.xz

$ cd .. && rm -rf packer
```

## usage

$ pacman -G <package>    #NOTE: get source

